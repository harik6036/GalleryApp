In this we will use MediaStream API- used for streaming , media recorder api for recording. Canvas api-for drawing api. HTML canvas apii for drawing.

video-collection of frames.
we need to collect frame and then capture it and download it. HTML Canvas Can be used to do drawings for drawing.
WEBGL is used for drawing 3-d drawing.
BLOB-BINARY LARGE OBJECT.

Indexdb api(Client-side storage)-we store key-value Pair in this. we can store complex objects,videos,blob,files.

open database.
create object store.
Perform operations.
